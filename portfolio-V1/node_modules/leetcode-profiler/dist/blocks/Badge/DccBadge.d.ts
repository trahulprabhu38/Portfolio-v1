import { HeatmapBadge } from '../../types';

interface DccBadgeProps {
    badge: HeatmapBadge;
    className?: string;
}
export declare const DccBadge: ({ badge, className }: DccBadgeProps) => import("react/jsx-runtime").JSX.Element;
export {};
