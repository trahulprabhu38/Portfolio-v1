import { Option } from '../../types';

interface DropdownProps {
    options: Option[];
    onOptionClick: (option: Option) => void;
    selectedOption: Option;
}
export declare const Dropdown: ({ options, onOptionClick, selectedOption, }: DropdownProps) => import("react/jsx-runtime").JSX.Element;
export {};
