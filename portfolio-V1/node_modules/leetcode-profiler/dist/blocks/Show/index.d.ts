import { default as React } from 'react';

interface ShowProps {
    children: React.ReactNode;
    when: boolean;
}
export declare const Show: ({ children, when }: ShowProps) => import("react/jsx-runtime").JSX.Element | null;
export {};
