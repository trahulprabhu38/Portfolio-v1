import { Badge } from '../../types';

interface AllBadgesModalProps {
    data: Badge[];
    showModal: boolean;
    toggleModal: () => void;
}
interface AllBadgesModalProps {
    data: Badge[];
    showModal: boolean;
    toggleModal: () => void;
}
export declare const AllBadgesModal: ({ data, showModal, toggleModal }: AllBadgesModalProps) => import("react/jsx-runtime").JSX.Element;
export {};
