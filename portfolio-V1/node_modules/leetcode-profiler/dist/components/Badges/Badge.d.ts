import { Badge } from '../../types';

interface BadgeProps {
    badge: Badge;
    className?: string;
    showFooter?: boolean;
}
export declare const LcBadge: ({ badge: { name, displayName, medal: { config: { iconGif, iconGifBackground }, }, icon, hoverText, creationDate, }, className, showFooter, }: BadgeProps) => import("react/jsx-runtime").JSX.Element | null;
export {};
