import { ContestRanking, UserContestRanking } from '../../../types';

interface ContestRatingGraphProps {
    userContestRankingHistory: ContestRanking[];
    startYear: number;
    currentYear: number;
    userContestDetails?: UserContestRanking;
}
export declare const ContestRatingGraph: ({ userContestRankingHistory, startYear, currentYear, userContestDetails, }: ContestRatingGraphProps) => import("react/jsx-runtime").JSX.Element;
export {};
