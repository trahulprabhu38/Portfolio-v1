import { Option, SubmissionCalendarResponse } from '../../types';

interface SubmissionsHeatmapProps {
    data?: SubmissionCalendarResponse;
    selectedYear: Option;
    setSelectedYear: (year: Option) => void;
}
export declare const SubmissionsHeatmap: ({ data, selectedYear, setSelectedYear, }: SubmissionsHeatmapProps) => import("react/jsx-runtime").JSX.Element;
export {};
