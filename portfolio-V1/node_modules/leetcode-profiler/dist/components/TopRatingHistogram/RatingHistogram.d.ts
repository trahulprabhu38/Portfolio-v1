import { RatingInfo } from '../../types';

interface RatingHistogramProps {
    data: any;
    topPercentage: any;
    ratingInfoData: RatingInfo[];
}
export declare const RatingHistogram: ({ data, topPercentage, ratingInfoData, }: RatingHistogramProps) => import("react/jsx-runtime").JSX.Element;
export {};
