import { ContestRanking, HighChartOptions } from '../types';

interface useHighChartOptionsProps {
    userContestRankingHistory: ContestRanking[];
    startYear: number;
    currentYear: number;
}
export declare const useHighChartOptions: ({ userContestRankingHistory, startYear, currentYear, }: useHighChartOptionsProps) => {
    options: HighChartOptions;
    currentHoveredIndex: number;
    isHovered: boolean;
};
export {};
