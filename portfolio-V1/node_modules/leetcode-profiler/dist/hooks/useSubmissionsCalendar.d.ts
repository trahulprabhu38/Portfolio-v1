import { Activity, DccBadge, HeatmapBadge, Option } from '../types';

export declare const useSubmissionsCalendar: (username: string, year?: string) => import('@tanstack/react-query').UseQueryResult<import('../types').SubmissionCalendarResponse, Error>;
export declare const useGetSubmissionsCalendarData: (data: string, selectedYear: Option) => {
    submissionCalendarData: {
        [year: number]: {
            [month: number]: Activity[];
        };
    };
    submissionsCount: number;
    currentMonth: number;
    currentYear: number;
};
export declare const useGetActiveYearsOptions: (activeYears: number[]) => Option[];
export declare const useTimestampToDccBadge: (dccBadges: DccBadge[]) => {
    [month: number]: HeatmapBadge;
};
