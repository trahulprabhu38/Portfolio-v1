import { TopRatingHistogramOptions } from '../types';

interface UseTopRatingHistogramProps {
    data: any[];
}
export declare const useTopRatingHistogramOptions: ({ data, }: UseTopRatingHistogramProps) => {
    options: TopRatingHistogramOptions;
    currentIndex: number;
    isHovered: boolean;
};
export {};
