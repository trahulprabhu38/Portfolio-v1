import { FunctionComponent } from "react";
import { type Props as ActivityCalendarProps } from "react-activity-calendar";
import { Activity, SubmissionCalendar, ThemeInput, Year } from "./types";
export interface Props extends Omit<ActivityCalendarProps, "data" | "theme"> {
    username: string;
    errorMessage?: string;
    theme?: ThemeInput;
    throwOnError?: boolean;
    transformData?: (data: SubmissionCalendar) => Array<Activity>;
    transformTotalCount?: boolean;
    year?: Year;
}
declare const LeetcodeCalendar: FunctionComponent<Props>;
export default LeetcodeCalendar;
